[package]
name = "distillery"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Name reservation. Distillery is the model-works port of the Mere platform: the harness where a ring of devices runs inference, embedding, and eventually training jobs. It surfaces the mesh job board, leases, device policy, and model manifests; esp is the seam it drives, the mesh is the queue it supervises."
readme = "README.md"
keywords = ["inference", "embedding", "mesh", "harness", "compute"]
categories = ["concurrency"]

[dependencies]
