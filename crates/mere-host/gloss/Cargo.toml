[package]
name = "mere-host-gloss"
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
description = "gpui renderer for mere's gloss pane — outline of an EngineDocument as an indented heading list."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
gpui = { workspace = true }
inker = { workspace = true }
