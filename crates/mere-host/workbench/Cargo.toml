[package]
name = "mere-host-workbench"
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
description = "gpui renderer for mere's workbench pane — turns inker EngineDocument blocks into a gpui element tree."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
gpui = { workspace = true }
inker = { workspace = true }
