# The toolchain rust-gpu pins, carried here because a shader crate is
# compiled by the codegen backend rather than by the host toolchain.
# Standard rust-gpu setup; it applies to this directory only.
[toolchain]
channel = "nightly-2026-05-22"
components = ["rust-src", "rustc-dev", "llvm-tools"]
