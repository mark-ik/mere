[package]
name = "graphshell-host"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Host-side adapters for Graphshell surfaces"

[lib]
path = "src/lib.rs"

[dependencies]
graphshell-core = { path = "../graphshell-core" }

[dev-dependencies]
graphshell-runtime = { path = "../graphshell-runtime" }
verso-tile = { path = "../../verso-tile" }
