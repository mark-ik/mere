[package]
name = "emblem"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Name reservation. An emblem is a graded, presentable proof of identity in the Mere platform's dramatis tier: made to be shown and surviving showing, from a bare key (continuity) through signed cross-attestations to delegation certificates. Your emblem is what someone else's gaz keeps. Not the secrets (that is chatelaine) and not the keeper (that is castellan)."
readme = "README.md"
keywords = ["identity", "attestation", "proof", "persona", "petname"]
categories = ["authentication", "cryptography"]

[dependencies]
