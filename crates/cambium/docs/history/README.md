# Historical implementation plans

These plans moved with the code they describe. They retain the original
`xilem-serval` and Chisel names so commit-era reasoning remains searchable.

Current ownership and names are defined by the repository root
`ARCHITECTURE.md`: `cambium` is the Serval backend, `meristem` is the reactive
core, and `sprigging` is the custom-leaf library.
