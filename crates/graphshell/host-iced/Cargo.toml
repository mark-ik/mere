[package]
name = "graphshell-host-iced"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Fresh iced host adapter boundary for Graphshell surfaces"

[lib]
path = "src/lib.rs"

[dependencies]
graphshell-core = { path = "../core" }
graphshell-host = { path = "../host" }
graphshell-runtime = { path = "../runtime" }