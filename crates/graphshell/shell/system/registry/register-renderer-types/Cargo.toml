[package]
name = "register-renderer-types"
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
publish = false
description = "Wasm-friendly data types for the renderer registry contract — InputEvent, NodeContentKind, RendererId, RendererCapabilities, etc. No vello / wgpu deps. Split from mere-renderer-registry so mere-host-runtime (and other wasm32-clean consumers) can consume the input vocabulary + scene contract without pulling the heavy renderer stack."
rust-version = "1.92.0"

[dependencies]
kurbo.workspace = true

[lints]
workspace = true
