Correction: this commit does not carry the change its message describes. roster::import_profile had already been swept into 311b61a8 ("Checkpoint the device-grant certificate split in flight") by a concurrent `git add -A` before this commit ran, so what landed here is only ports/graphshell/src/native/owner_settings.rs, which belongs to that other work. The import_profile design notes in the message above are accurate; look for the code in 311b61a8.
